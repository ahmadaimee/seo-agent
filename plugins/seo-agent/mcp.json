{
  "mcpServers": {
    "seo-agent": {
      "type": "http",
      "url": "${SEO_AGENT_MCP_URL:-http://localhost:3001/mcp}",
      "headers": {
        "Authorization": "${SEO_AGENT_MCP_AUTH:-}"
      }
    }
  }
}
